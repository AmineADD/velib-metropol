
import { makeStyles } from '@mui/styles';

export default makeStyles({
    container: {
        display: "flex",
        justifyContent: "center",
        gap: "20px",
        alignItems: "center",
    },
    stats: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        gap: "5px"
    }
});